version: '3'
services:
  sweetforge:
    build: .
    container_name: sweetforge-kley
    restart: unless-stopped
    networks:
      - sweetforge
    volumes:
      - ./results:/results

networks:
  sweetforge:
    external: true
    name: sweetforge
